// this is where the tests for the employee class will go
const Employee = require('../lib/Employee')

describe("employee", () => {
    describe("initialization", () => {
        it("should", () => {
            // hardcode variables to check function

            expect(" ").toEqual(" ");
        });

        // make test for input and data collected
    })
})